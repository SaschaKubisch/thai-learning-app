# v0.4.1 -- Remove Mastered/Learned Stats

**Release date:** 2026-03-03
**Tag:** v0.4.1

## Summary

Hotfix to remove the misleading "0 mastered" and "56 learned" labels from the home screen progress section. The progress card now shows only the progress bar with "X of 30 completed."

## Changes

### UI

- Removed "mastered" and "learned" counters from the progress card
- Cleaned up unused `getStats` import and `stats` variable from HomeScreen

## Files modified

- `src/components/HomeScreen.tsx` -- removed stats display and unused imports

## Verification

- TypeScript: `npx tsc --noEmit` passes
- Build: `npm run build` succeeds
- Tests: all E2E tests pass
